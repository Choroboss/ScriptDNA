# Project Agent Rules

## GOLDEN RULE (REGLA DE ORO)
- **Continuous Creator Learning**: Regardless of which LLM provider or model is used for generation, the system MUST always query the creator's ingested training transcripts to extract real sentence structures, vocabulary signatures, hooks, and rhythm.
- This learned context MUST be dynamically injected into every script generation and refinement prompt.
